This is a Terminal UI app build using React Ink which uses Github Copilot CLI as its executor.

The general idea of the app is that it provides a list of tasks that user can add that will create a prd.json file and a loop executes the task one by one using github copilot cli.